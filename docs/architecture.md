# System Architecture

The project architecture represents a small enterprise environment
designed for adversary simulation.

## Architecture Layers
- Network layer (VirtualBox NAT Network)
- Identity layer (Active Directory)
- Endpoint layer (Windows and Linux machines)
- Attack simulation layer (MITRE CALDERA)

The architecture allows realistic attack paths
from an external attacker to internal assets.
